[package]
name = "escrow_marketplace"
version = "0.1.0"
description = "Peer-to-peer marketplace (property + general items) with SOL escrow and AI-assisted listing review"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "escrow_marketplace"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
